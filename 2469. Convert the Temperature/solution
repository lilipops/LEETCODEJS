/**
 * @param {number} celsius
 * @return {number[]}
 */
var convertTemperature = function(celsius) {
    let solution = []
    solution[0] = celsius + 273.15
    solution[1] = (celsius * 1.80 + 32.00)
    return solution
};
